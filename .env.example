# Optional: enables the real n8n + LLM analysis route.
N8N_WEBHOOK_URL=http://localhost:5678/webhook/sentiment-analyze

# Optional: only needed if the frontend is hosted on a different origin.
CLIENT_ORIGIN=http://127.0.0.1:5173
